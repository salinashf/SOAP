<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="ec.edu.uisrael.wslocalizacion0002.MainActivity" >

    <TextView
        android:id="@+id/lblInput"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/hello_world" />

    <Button
        android:id="@+id/bntExeServ"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/lblInput"
        android:layout_marginTop="18dp"
        android:text="@string/ejecutarServicio" />

    <EditText
        android:id="@+id/txtOuotSoap"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/bntExeServ"
        android:ems="10"
        android:inputType="textMultiLine"
        android:lines="25" >

        <requestFocus />
    </EditText>

</RelativeLayout>